### Bug Fixes

- The installer no longer warns about closing other programs or rebooting. That warning was leftover boilerplate; this install is per-user and never touches shared files or needs a restart.
- The download page no longer includes a second file that looked like another way to install the app. Only the installer is meant to be run directly now.
